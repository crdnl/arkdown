extends ../layout

include ../mixin/pagination

block content
	h1= title
	hr
	
	.section
		.row
			each card in results.docs
				.col.s12.m6.l4
					include card
		.center
			+pagination(1, results.pages, results.page, "/" + base + "/")
			